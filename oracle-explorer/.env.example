# Oracle Explorer - Environment Variables
# Copy this file to .env.local and adjust paths for your local setup

# Network to use (local, devnet, or testnet)
# Default: testnet
NETWORK=testnet

# Path to the oracle workspace (relative to project root)
# For testnet/devnet: ../.demo-workspaces/oracle
# For local: ../local-node
ORACLE_WORKSPACE_PATH=../.demo-workspaces/oracle

# Path to the compiled CLI binaries (relative to project root)
# Default: ../target/release
CLI_PATH=../target/release

# Alternative with absolute paths (example):
# ORACLE_WORKSPACE_PATH=/path/to/pragma-miden/.demo-workspaces/oracle
# CLI_PATH=/path/to/pragma-miden/target/release

# Example for local network:
# NETWORK=local
# ORACLE_WORKSPACE_PATH=../local-node

# Pragma API Configuration (for historical data seeding)
# Get your API key from: https://pragma.build
# Historical charts available for BTC/USD and ETH/USD only
# Other assets will display live prices without historical charts
PRAGMA_API_KEY=

# Pragma API Base URL (optional, defaults to production)
# Default: https://api.production.pragma.build/node/v1/onchain/history
# PRAGMA_API_BASE_URL=https://api.devnet.pragma.build/node/v1/onchain/history
